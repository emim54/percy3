version: 2
snapshot:
  # Figma-Snapshot als Baseline festlegen
  - name: figma-snapshot
    # Angeben, dass es sich um eine Baseline handelt
    baseline: true
    # Dateipfade zum Figma-Snapshot angeben
    paths:
      - "C:\Users\emrei\IdeaProjects\figma\figma.yml"

# Snapshot-Konfiguration für Cypress-Tests
snapshot:
  # Cypress-Snapshot als Änderungen festlegen
  - name: cypress-snapshot
    # Angeben, dass es sich um Änderungen im Vergleich zur Baseline handelt
    diff: true
    # Dateipfade zum Cypress-Snapshot angeben
    paths:
      - "C:\Users\emrei\IdeaProjects\figma\cypress\e2e\figma.cy.js"